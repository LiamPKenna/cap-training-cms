import React from "react";

const Loading = () => {
  const loadStyle = {
    paddingTop: "6rem",
    textAlign: "center"
  };
  return (
    <div style={loadStyle}>
      <h2>LOADING...</h2>
    </div>
  );
};

export default Loading;
